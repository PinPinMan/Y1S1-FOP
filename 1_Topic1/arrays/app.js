// arr = different elements of the same array can be of different types: string, boolean, even objects or other arrays

// var arr = [];
// var arr = new Array();
// var arr = new Array(10);  - arr = undefined*10


// arr[4] = arr["4"]

// string.push(); 		    = does nothing to list
// string.pop();            = last item

// X = string.push(item);	= new string length  (with item)
// X = string.pop();        = last item